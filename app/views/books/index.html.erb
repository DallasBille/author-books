<h1>Books!</h1>
    <ul>
        <%@books.each do |book|%>
            <li>
                <%=link_to book.name, book_path(book)%> <%="(#{book.pages} pages)"%>
            </li>
        <%end%>
    </ul>
